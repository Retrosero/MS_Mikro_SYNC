# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# JWT_SECRET: Secret used to sign JSON Web Tokens
JWT_SECRET="replace-this-with-a-very-long-random-string-for-production"

# SYNC LIMITS
# Large ERP bulk payloads need more than Express' small default body limits.
JSON_BODY_LIMIT="25mb"
SYNC_RATE_LIMIT_MAX="2000"

# DATABASE CONFIGURATION
# DB_CLIENT: Choose between 'pg' (PostgreSQL), 'mysql2' (MySQL/MariaDB), or 'better-sqlite3' (SQLite - Default)
DB_CLIENT="better-sqlite3"

# Option A: Connection via a single URL (Highly recommended for PostgreSQL/MySQL on Coolify/Render/Railway)
# DATABASE_URL="postgres://username:password@hostname:5432/databasename"
# DATABASE_URL="mysql://username:password@hostname:3306/databasename"

# Option B: Connection via individual variables
# DB_HOST="127.0.0.1"
# DB_PORT=5432
# DB_USER="my_user"
# DB_PASSWORD="my_password"
# DB_NAME="my_database"
# DB_SSL="false"

# BRIDGE: Sync Adapter entegrasyonu (opsiyonel)
# MS_MikroYNC admin panelinden uretilen api_key, bu URL'deki Sync Adapter'a otomatik aktarilir.
# Bos birakirsan webhook devre disi kalir (mevcut davranis korunur).
SYNC_ADAPTER_URL="https://api.syncadapter.example.com"
SYNC_ADAPTER_BRIDGE_SECRET="degistir-benimle-paylasma"
